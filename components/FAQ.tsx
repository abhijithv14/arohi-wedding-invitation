"use client";

// FAQ is intentionally disabled because the wedding site no longer includes
// an FAQ section. This file is kept as a harmless placeholder so stale imports
// can be removed without causing a TypeScript build failure.
export default function FAQ() {
  return null;
}
